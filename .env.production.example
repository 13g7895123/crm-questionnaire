# ===========================================
# Production Environment Configuration
# 正式環境配置範例
# ===========================================

# Application
APP_ENV=production

# Ports
BACKEND_PORT=9104
DB_PORT=9213
PMA_PORT=8081

# Database
DB_HOST=db
DB_ROOT_PASSWORD=change_this_in_production
DB_DATABASE=crm_questionnaire
DB_USERNAME=crm_user
DB_PASSWORD=change_this_in_production

# Cookie Security Settings (正式環境 - 使用 HTTPS)
COOKIE_SECURE=true
COOKIE_HTTPONLY=true
COOKIE_SAMESITE=Lax
COOKIE_DOMAIN=crm.l

# CORS Settings (正式環境 - 僅允許正式域名)
CORS_ALLOWED_ORIGINS=https://crm.l
